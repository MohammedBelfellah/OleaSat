services:
  api:
    build: .
    container_name: oleasat-api
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - oleasat-data:/app/data

volumes:
  oleasat-data:
